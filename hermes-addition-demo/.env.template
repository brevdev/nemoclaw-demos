# ──────────────────────────────────────────────────────────────
# Hermes Agent Demo — NemoClaw environment config
# Copy this file to .env and fill in your values before running install.sh
# ──────────────────────────────────────────────────────────────

# Required: your NVIDIA Inference API key (or whichever provider you use)
INFERENCE_API_KEY=

# Optional: inference provider settings ( tested with nvidia as provider)
INFERENCE_PROVIDER_TYPE=
INFERENCE_PROVIDER_NAME=
INFERENCE_BASE_URL=
INFERENCE_MODEL=
